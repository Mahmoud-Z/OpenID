- Demo Simulation (Full Cycle)
  IP: core
  MVC: (cookies auth like FATORTY or Identity) 
  json file storage (user data) or static list memory
  
  
- FATORTY
  pre-configured data (client & IP) in secure way (DB or web.conf (encrypted) or encironment variables)
  
  //Tranfer Demo code to Fatorty
  nuget openid
  ....


new table encrypted for openID Data
if(check if database can be seen from startup)
nice
else
check a way to do this

  